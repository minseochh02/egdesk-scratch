// ============================================================================
// KB CARD MODULE
// ============================================================================

const { KBCardAutomator, createKBCardAutomator, runKBCardAutomation } = require('./KBCardAutomator');
const { KB_CARD_INFO, KB_CARD_CONFIG } = require('./config');

module.exports = {
  KBCardAutomator,
  createKBCardAutomator,
  runKBCardAutomation,
  KB_CARD_INFO,
  KB_CARD_CONFIG,
};
