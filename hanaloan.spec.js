/**
 * RECORDED_ACTIONS:
 * [{"type":"navigate","url":"https://biz.kebhana.com/index.jsp","timestamp":3741},{"type":"click","selector":".ui-popup_child_button-close:nth-match(1)","xpath":"/html/body/div[2]/div[2]/div/div[2]/div[1]/div[1]/div[4]/button","value":"팝업 닫기","innerText":"팝업 닫기","role":"button","ariaLabel":"","timestamp":10316},{"type":"click","selector":"button:has-text(\"팝업 닫기\")","value":"팝업 닫기","timestamp":10318,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".btn:nth-match(24)","xpath":"/html/body/div[2]/div[2]/div/div[1]/section/div/div[3]/div[1]/button[1]","value":"로그인","innerText":"로그인","role":"button","ariaLabel":"","timestamp":11556},{"type":"click","selector":"button:has-text(\"로그인\")","value":"로그인","timestamp":11557,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"[id=\"certLogin\"]","value":"로그인","timestamp":15990,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".ui-popup_child_button-close:nth-match(1)","xpath":"/html/body/div[1]/div[2]/div[4]/button","value":"팝업 닫기","innerText":"팝업 닫기","role":"button","ariaLabel":"","timestamp":36693},{"type":"click","selector":"button:has-text(\"팝업 닫기\")","value":"팝업 닫기","timestamp":36694,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"[id=\"40000\"]","value":"상품가입 · 업무","timestamp":40245,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"a:nth-match(645)","xpath":"/html/body/div[2]/div[2]/div[2]/div[2]/div[2]/div[1]/div[1]/ul/li[7]/div/div/div[2]/ul/li[3]/a","value":"대출 거래내역/대출계산서 조회","innerText":"대출 거래내역/대출계산서 조회","role":"a","ariaLabel":"","timestamp":49880},{"type":"click","selector":"a:has-text(\"대출 거래내역/대출계산서 조회\")","value":"대출 거래내역/대출계산서 조회","timestamp":49881,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".selected:nth-match(1)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/div","value":"계좌선택","innerText":"계좌선택","role":"div","ariaLabel":"","timestamp":59582},{"type":"click","selector":".selected","value":"계좌선택","timestamp":59584,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"li:nth-match(2043)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/ol/li[2]","value":"213-980073-29742\n [기업운전일반자금대출]","innerText":"213-980073-29742\n[기업운전일반자금대출]","role":"li","ariaLabel":"","timestamp":60848},{"type":"click","selector":"li","value":"213-980073-29742\n [기업운전일반자금대출]","timestamp":60849,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".seltitle:nth-match(1)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/div/span[1]","value":"213-980073-29742","innerText":"213-980073-29742","role":"span","ariaLabel":"","timestamp":63756},{"type":"click","selector":".seltitle","value":"213-980073-29742","timestamp":63758,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".seltitle:nth-match(4)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/ol/li[3]/span[1]","value":"213-980083-81342","innerText":"213-980083-81342","role":"span","ariaLabel":"","timestamp":64670},{"type":"click","selector":".seltitle","value":"213-980083-81342","timestamp":64672,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"span:nth-match(90)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/div/span[2]","value":"[기업시설일반자금대출]","innerText":"[기업시설일반자금대출]","role":"span","ariaLabel":"","timestamp":67866},{"type":"click","selector":"span","value":"[기업시설일반자금대출]","timestamp":67868,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"span:nth-match(94)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/ol/li[2]/span[2]","value":"[기업운전일반자금대출]","innerText":"[기업운전일반자금대출]","role":"span","ariaLabel":"","timestamp":69486},{"type":"click","selector":"span","value":"[기업운전일반자금대출]","timestamp":69488,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"[id=\"numSearchDiv\"]","value":"실행번호 검색","timestamp":70358,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".GMWrap0:nth-match(5)","xpath":"/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[2]/td[1]/div/table/tbody/tr[2]/td[4]","value":"실행번호","innerText":"실행번호","role":"td","ariaLabel":"","timestamp":85629},{"type":"click","selector":".","value":"실행번호","timestamp":85631,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".GMClassReadOnly:nth-match(2)","xpath":"/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[3]/td/div/div[1]/table/tbody/tr[2]/td[4]","value":"13","innerText":"13","role":"td","ariaLabel":"","timestamp":86962},{"type":"click","selector":".","value":"13","timestamp":86965,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".GMWrap0:nth-match(4)","xpath":"/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[2]/td[1]/div/table/tbody/tr[2]/td[3]","value":"선택","innerText":"선택","role":"td","ariaLabel":"","timestamp":89555},{"type":"click","selector":".","value":"선택","timestamp":89556,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".GMWrap0:nth-match(12)","xpath":"/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[3]/td/div/div[1]/table/tbody/tr[2]/td[3]","value":"","innerText":"","role":"td","ariaLabel":"","timestamp":91055},{"type":"click","selector":".","value":"","timestamp":91058,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"fill","selector":"[id=\"tmpinput1\"]","value":"on","timestamp":99354,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".btn:nth-match(1)","xpath":"/html/body/div[1]/div[2]/div[3]/a[1]","value":"확인","innerText":"확인","role":"a","ariaLabel":"","timestamp":99452},{"type":"click","selector":"a:has-text(\"확인\")","value":"확인","timestamp":99454,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".btn:nth-match(2)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[2]/a","value":"조회","innerText":"조회","role":"a","ariaLabel":"","timestamp":103438},{"type":"click","selector":"a:has-text(\"조회\")","value":"조회","timestamp":103440,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".btn_calendar:nth-match(1)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td/span[1]/span","value":"달력 시작일","innerText":"달력 시작일","role":"span","ariaLabel":"","timestamp":105883},{"type":"click","selector":".btn_calendar","value":"달력 시작일","timestamp":105886,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"button:nth-match(5)","xpath":"/html/body/div[1]/div[2]/div[3]/table/tbody/tr[1]/td[6]/button","value":"1","innerText":"1","role":"button","ariaLabel":"","timestamp":108433},{"type":"click","selector":"button:has-text(\"1\")","value":"1","timestamp":108435,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"fill","selector":"[id=\"inqStrDt\"]","value":"2026-05-01","timestamp":109305,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".btn_calendar:nth-match(2)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td/span[2]/span","value":"달력 종료일","innerText":"달력 종료일","role":"span","ariaLabel":"","timestamp":109425},{"type":"click","selector":".btn_calendar","value":"달력 종료일","timestamp":109427,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"button:nth-match(33)","xpath":"/html/body/div[1]/div[2]/div[3]/table/tbody/tr[5]/td[6]/button","value":"29","innerText":"29","role":"button","ariaLabel":"","timestamp":110699},{"type":"click","selector":"button:has-text(\"29\")","value":"29","timestamp":110701,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"fill","selector":"[id=\"inqEndDt\"]","value":"2026-05-29","timestamp":113054,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"[id=\"inqStrDt\"]","value":"","timestamp":113086,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"[id=\"inqStrDt\"]","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td/span[1]/input","value":"","innerText":"","role":"input","ariaLabel":"","timestamp":113413},{"type":"click","selector":"tr:nth-match(4)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]","value":"조회기간\n\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t 오늘 \n 7일 \n 15일 \n 1개월 \n 3개월 \n 6개월 \n 12개월 \n\n월별 선택2026년 05월2026년 04월2026년 03월2026년 02월2026년 01월2025년 12월2025년 11월2025년 10월2025년 09월2025년 08월2025년 07월2025년 06월2025년 05월\n\n\n\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 시작일\n\t\t\t\t\t\t\t\t\t ~\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 종료일\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t직접입력 예시 : 20171229","innerText":"조회기간\t\n오늘\n7일\n15일\n1개월\n3개월\n6개월\n12개월\n월별 선택\n2026년 05월\n2026년 04월\n2026년 03월\n2026년 02월\n2026년 01월\n2025년 12월\n2025년 11월\n2025년 10월\n2025년 09월\n2025년 08월\n2025년 07월\n2025년 06월\n2025년 05월\n달력 시작일\n ~ \n달력 종료일\n 직접입력 예시 : 20171229","role":"tr","ariaLabel":"","timestamp":114187},{"type":"click","selector":"tr","value":"조회기간\n\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t 오늘 \n 7일 \n 15일 \n 1개월 \n 3개월 \n 6개월 \n 12개월 \n\n월별 선택2026년 05월2026년 04월2026년 03월2026년 02월2026년 01월2025년 12월2025년 11월2025년 10월2025년 09월2025년 08월2025년 07월2025년 06월2025년 05월\n\n\n\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 시작일\n\t\t\t\t\t\t\t\t\t ~\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 종료일\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t직접입력 예시 : 20171229","timestamp":114189,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"fill","selector":"[id=\"inqStrDt\"]","value":"2026-05-00","timestamp":118472,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"fill","selector":"[id=\"inqStrDt\"]","value":"2026-05-01","timestamp":120855,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"[id=\"inqEndDt\"]","value":"","timestamp":122883,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"td:nth-match(4)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td","value":"오늘 \n 7일 \n 15일 \n 1개월 \n 3개월 \n 6개월 \n 12개월 \n\n월별 선택2026년 05월2026년 04월2026년 03월2026년 02월2026년 01월2025년 12월2025년 11월2025년 10월2025년 09월2025년 08월2025년 07월2025년 06월2025년 05월\n\n\n\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 시작일\n\t\t\t\t\t\t\t\t\t ~\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 종료일\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t직접입력 예시 : 20171229","innerText":"오늘\n7일\n15일\n1개월\n3개월\n6개월\n12개월\n월별 선택\n2026년 05월\n2026년 04월\n2026년 03월\n2026년 02월\n2026년 01월\n2025년 12월\n2025년 11월\n2025년 10월\n2025년 09월\n2025년 08월\n2025년 07월\n2025년 06월\n2025년 05월\n달력 시작일\n ~ \n달력 종료일\n 직접입력 예시 : 20171229","role":"td","ariaLabel":"","timestamp":124156},{"type":"click","selector":"td","value":"오늘 \n 7일 \n 15일 \n 1개월 \n 3개월 \n 6개월 \n 12개월 \n\n월별 선택2026년 05월2026년 04월2026년 03월2026년 02월2026년 01월2025년 12월2025년 11월2025년 10월2025년 09월2025년 08월2025년 07월2025년 06월2025년 05월\n\n\n\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 시작일\n\t\t\t\t\t\t\t\t\t ~\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t\t달력 종료일\n\t\t\t\t\t\t\t\t\t\n\t\t\t\t\t\t\t\t\t직접입력 예시 : 20171229","timestamp":124159,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":"[id=\"inqEndDt\"]","value":"","timestamp":146622,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".btn:nth-match(2)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[2]/a","value":"조회","innerText":"조회","role":"a","ariaLabel":"","timestamp":150431},{"type":"click","selector":"a:has-text(\"조회\")","value":"조회","timestamp":150433,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"click","selector":".btn:nth-match(7)","xpath":"/html/body/div[2]/div[2]/div[3]/div[2]/div/div[3]/div[2]/a[5]","value":"전체엑셀다운로드","innerText":"전체엑셀다운로드","role":"a","ariaLabel":"","timestamp":158814},{"type":"click","selector":"a:has-text(\"전체엑셀다운로드\")","value":"전체엑셀다운로드","timestamp":158817,"frameSelector":"[id=\"hanaMainframe\"]"},{"type":"download","selector":"download-wait","value":"Download started: 거래내역_20260529.xls","timestamp":159283},{"type":"download","selector":"download-complete","value":"Download completed: 거래내역_20260529.xls","timestamp":159616}]
 *
 * CHROME_EXTENSIONS:
 * []
 */

const { chromium } = require('playwright-core');
const path = require('path');
const os = require('os');
const fs = require('fs');

// Original extension paths from recording session
const EXTENSION_PATHS = [];

/**
 * Copy Chrome extensions to temporary directory
 * Returns { paths: string[], tempDir: string }
 */
function copyExtensionsToTemp(profileDir) {
  if (EXTENSION_PATHS.length === 0) return { paths: [], tempDir: null };
  
  try {
    const tempExtensionsDir = fs.mkdtempSync(path.join(os.tmpdir(), 'egdesk-extensions-'));
    console.log('[Replay] Copying', EXTENSION_PATHS.length, 'extensions to:', tempExtensionsDir);
    
    const copiedPaths = [];
    
    for (const extPath of EXTENSION_PATHS) {
      if (!fs.existsSync(extPath)) {
        console.warn('[Replay] Extension not found:', extPath);
        continue;
      }
      
      const version = path.basename(extPath);
      const extensionId = path.basename(path.dirname(extPath));
      const destPath = path.join(tempExtensionsDir, `${extensionId}-${version}`);
      
      fs.cpSync(extPath, destPath, { recursive: true });
      copiedPaths.push(destPath);
      console.log('[Replay] ✓ Copied:', extensionId);
    }
    
    // Copy native messaging hosts
    copyNativeMessagingHosts(profileDir);
    
    console.log('[Replay] Successfully copied', copiedPaths.length + '/' + EXTENSION_PATHS.length, 'extensions');
    return { paths: copiedPaths, tempDir: tempExtensionsDir };
  } catch (error) {
    console.error('[Replay] Error copying extensions:', error);
    return { paths: [], tempDir: null };
  }
}

/**
 * Copy native messaging host manifests to profile directory
 */
function copyNativeMessagingHosts(profileDir) {
  try {
    const nativeHostLocations = [
      '/Library/Google/Chrome/NativeMessagingHosts',
      '/Library/Application Support/Google/Chrome/NativeMessagingHosts',
      path.join(os.homedir(), 'Library/Application Support/Google/Chrome/NativeMessagingHosts'),
      '/Library/Application Support/Chromium/NativeMessagingHosts',
      path.join(os.homedir(), 'Library/Application Support/Chromium/NativeMessagingHosts')
    ];
    
    const destNativeHostsDir = path.join(profileDir, 'NativeMessagingHosts');
    if (!fs.existsSync(destNativeHostsDir)) {
      fs.mkdirSync(destNativeHostsDir, { recursive: true });
    }
    
    let copiedCount = 0;
    for (const location of nativeHostLocations) {
      if (fs.existsSync(location)) {
        const files = fs.readdirSync(location);
        for (const file of files) {
          if (file.endsWith('.json')) {
            const sourcePath = path.join(location, file);
            const destPath = path.join(destNativeHostsDir, file);
            if (!fs.existsSync(destPath)) {
              fs.copyFileSync(sourcePath, destPath);
              copiedCount++;
            }
          }
        }
      }
    }
    
    if (copiedCount > 0) {
      console.log(`[Replay] ✓ Copied ${copiedCount} native messaging host(s)`);
    }
  } catch (error) {
    console.warn('[Replay] Error copying native hosts:', error);
  }
}

/** Persist preferredLocatorStrategy into this file's RECORDED_ACTIONS comment after a successful run */
function __egdeskMergeSelectorPrefsIntoSpec(filePath, learnMap) {
  if (!learnMap || typeof learnMap !== 'object') return;
  const ks = Object.keys(learnMap);
  if (ks.length === 0) return;
  try {
    const raw = fs.readFileSync(filePath, 'utf8');
    const marker = 'RECORDED_ACTIONS:';
    const idx = raw.indexOf(marker);
    if (idx === -1) return;
    let pos = idx + marker.length;
    while (pos < raw.length && /\s/.test(raw[pos])) pos++;
    while (pos < raw.length && (raw[pos] === '*' || raw[pos] === ' ')) {
      if (raw[pos] === '*') { pos++; while (pos < raw.length && raw[pos] === ' ') pos++; }
      else pos++;
    }
    if (pos >= raw.length || raw[pos] !== '[') return;
    const startBracket = pos;
    let depth = 0, inString = false, stringQuote = null, escape = false;
    let endBracket = -1;
    for (let i = startBracket; i < raw.length; i++) {
      const c = raw[i];
      if (inString) {
        if (escape) { escape = false; continue; }
        if (c === '\') { escape = true; continue; }
        if (stringQuote && c === stringQuote) { inString = false; stringQuote = null; }
        continue;
      }
      if (c === '"' || c === "'") { inString = true; stringQuote = c; continue; }
      if (c === '[') depth++;
      if (c === ']') {
        depth--;
        if (depth === 0) { endBracket = i + 1; break; }
      }
    }
    if (endBracket < 0) return;
    const jsonStr = raw.slice(startBracket, endBracket);
    const actions = JSON.parse(jsonStr);
    for (const k of ks) {
      const ix = parseInt(k, 10);
      if (actions[ix]) actions[ix].preferredLocatorStrategy = learnMap[k];
    }
    const newJson = JSON.stringify(actions);
    fs.writeFileSync(filePath, raw.slice(0, startBracket) + newJson + raw.slice(endBracket), 'utf8');
    console.log('[Replay] 📝 Saved preferredLocatorStrategy for', ks.length, 'action(s)');
  } catch (e) {
    console.warn('[Replay] Could not persist selector preferences:', e && e.message);
  }
}

(async () => {
  console.log('🎬 Starting test replay...');
  
  // Create downloads directory in system Downloads folder (grouped under EGDesk-Browser)
  const downloadsPath = path.join(os.homedir(), 'Downloads', 'EGDesk-Browser', 'hanaloan');
  if (!fs.existsSync(downloadsPath)) {
    fs.mkdirSync(downloadsPath, { recursive: true });
  }
  console.log('📥 Downloads will be saved to:', downloadsPath);

  // Create temporary profile directory
  const profileDir = fs.mkdtempSync(path.join(os.tmpdir(), 'playwright-profile-'));
  console.log('📁 Using profile directory:', profileDir);

  // Copy Chrome extensions if any were used during recording
  let copiedExtensionPaths = [];
  let tempExtensionsDir = null;
  if (EXTENSION_PATHS.length > 0) {
    const extensionResult = copyExtensionsToTemp(profileDir);
    copiedExtensionPaths = extensionResult.paths;
    tempExtensionsDir = extensionResult.tempDir;
  }

  // Launch browser with persistent context (more reliable in production)
  const browserChannel = copiedExtensionPaths.length > 0 ? 'chromium' : 'chrome';
  console.log(`🎭 Launching with channel: ${browserChannel}`);
  if (copiedExtensionPaths.length > 0) {
    console.log('🧩 Loading', copiedExtensionPaths.length, 'Chrome extension(s)');
  }

  // Build browser args
  const args = [
    '--window-size=1152,1032',
    '--window-position=768,0',
    '--no-default-browser-check',
    '--disable-blink-features=AutomationControlled',
    '--no-first-run',
    // Permission handling for localhost and private network access
    '--disable-web-security',
    '--disable-features=IsolateOrigins,site-per-process',
    '--allow-running-insecure-content',
    '--disable-features=PrivateNetworkAccessSendPreflights',
    '--disable-features=PrivateNetworkAccessRespectPreflightResults'
  ];

  // Add extension loading args if extensions were copied
  if (copiedExtensionPaths.length > 0) {
    const extensionPathsStr = copiedExtensionPaths.join(',');
    args.push(`--disable-extensions-except=${extensionPathsStr}`);
    args.push(`--load-extension=${extensionPathsStr}`);
  }

  const context = await chromium.launchPersistentContext(profileDir, {
    headless: false,
    channel: browserChannel,
    viewport: null,
    permissions: ['clipboard-read', 'clipboard-write'],
    acceptDownloads: true,
    downloadsPath: downloadsPath,
    args: args
  });

  // Get or create page
  const pages = context.pages();
  let page = pages.length > 0 ? pages[0] : await context.newPage(); // Use 'let' to allow tab switching
  const pageStack = []; // Track page history for popup close handling

  // Set up dialog handling (auto-accept alerts/confirms for downloads)
  page.on('dialog', async (dialog) => {
    console.log(`🔔 Dialog detected: ${dialog.type()} - "${dialog.message()}"`);
    await dialog.accept();
    console.log('✅ Dialog accepted');
  });

  try {
    const __selectorLearn = {};
    function __egdeskRecordStrategy(actionIndex, strategy) {
      if (strategy) __selectorLearn[actionIndex] = strategy;
    }

    await page.goto('https://biz.kebhana.com/index.jsp');
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('button', { name: '팝업 닫기' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(1, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.ui-popup_child_button-close:nth-match(1)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(1, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div/div[2]/div[1]/div[1]/div[4]/button');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(1, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('button:has-text("팝업 닫기")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(2, 'css');
    }
    await page.waitForTimeout(1238); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('button', { name: '로그인' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(3, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn:nth-match(24)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(3, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div/div[1]/section/div/div[3]/div[1]/button[1]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(3, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('button:has-text("로그인")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(4, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('[id="certLogin"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(5, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('button', { name: '팝업 닫기' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(6, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.ui-popup_child_button-close:nth-match(1)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(6, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[4]/button');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(6, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('button:has-text("팝업 닫기")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(7, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('[id="40000"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(8, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '대출 거래내역/대출계산서 조회' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(9, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(645)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(9, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[2]/div[2]/div[2]/div[1]/div[1]/ul/li[7]/div/div/div[2]/ul/li[3]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(9, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('a:has-text("대출 거래내역/대출계산서 조회")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(10, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('div', { name: '계좌선택' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(11, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.selected:nth-match(1)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(11, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/div');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(11, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.selected');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(12, 'css');
    }
    await page.waitForTimeout(1264); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('li', { name: '213-980073-29742\n[기업운전일반자금대출]' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(13, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('li:nth-match(2043)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(13, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/ol/li[2]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(13, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('li');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(14, 'css');
    }
    await page.waitForTimeout(2907); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('span', { name: '213-980073-29742' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(15, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.seltitle:nth-match(1)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(15, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/div/span[1]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(15, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.seltitle');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(16, 'css');
    }
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('span', { name: '213-980083-81342' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(17, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.seltitle:nth-match(4)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(17, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/ol/li[3]/span[1]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(17, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.seltitle');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(18, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('span', { name: '[기업시설일반자금대출]' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(19, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('span:nth-match(90)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(19, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/div/span[2]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(19, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('span');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(20, 'css');
    }
    await page.waitForTimeout(1618); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('span', { name: '[기업운전일반자금대출]' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(21, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('span:nth-match(94)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(21, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[1]/td/div[1]/span/div[2]/ol/li[2]/span[2]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(21, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('span');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(22, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('[id="numSearchDiv"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(23, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('td', { name: '실행번호' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(24, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.GMWrap0:nth-match(5)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(24, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[2]/td[1]/div/table/tbody/tr[2]/td[4]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(24, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(25, 'css');
    }
    await page.waitForTimeout(1331); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('td', { name: '13' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(26, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.GMClassReadOnly:nth-match(2)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(26, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[3]/td/div/div[1]/table/tbody/tr[2]/td[4]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(26, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(27, 'css');
    }
    await page.waitForTimeout(2590); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('td', { name: '선택' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(28, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.GMWrap0:nth-match(4)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(28, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[2]/td[1]/div/table/tbody/tr[2]/td[3]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(28, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(29, 'css');
    }
    await page.waitForTimeout(1499); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css → xpath)
    try {
      // CSS selector
      const locator = page.locator('.GMWrap0:nth-match(12)');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(30, 'css');
    } catch (error0) {
      try {
        // XPath
        const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[2]/div/div[2]/div[2]/div[1]/div/div/div/div/table/tbody/tr[3]/td/div/div[1]/table/tbody/tr[2]/td[3]');
        await xpathLocator.hover({ force: true });
        await xpathLocator.click();
        __egdeskRecordStrategy(30, 'xpath');
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(31, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    await page.frameLocator('[id="hanaMainframe"]').locator('[id="tmpinput1"]').fill('on'); // Fill in iframe
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '확인' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(33, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn:nth-match(1)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(33, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[3]/a[1]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(33, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('a:has-text("확인")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(34, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '조회' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(35, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn:nth-match(2)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(35, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[2]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(35, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('a:has-text("조회")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(36, 'css');
    }
    await page.waitForTimeout(2443); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('span', { name: '달력 시작일' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(37, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn_calendar:nth-match(1)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(37, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td/span[1]/span');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(37, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.btn_calendar');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(38, 'css');
    }
    await page.waitForTimeout(2547); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('button', { name: '1' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(39, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('button:nth-match(5)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(39, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[3]/table/tbody/tr[1]/td[6]/button');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(39, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('button:has-text("1")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(40, 'css');
    }
    await page.frameLocator('[id="hanaMainframe"]').locator('[id="inqStrDt"]').fill('2026-05-01'); // Fill in iframe
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('span', { name: '달력 종료일' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(42, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn_calendar:nth-match(2)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(42, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td/span[2]/span');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(42, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('.btn_calendar');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(43, 'css');
    }
    await page.waitForTimeout(1272); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('button', { name: '29' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(44, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('button:nth-match(33)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(44, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[1]/div[2]/div[3]/table/tbody/tr[5]/td[6]/button');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(44, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('button:has-text("29")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(45, 'css');
    }
    await page.waitForTimeout(2353); // Human-like delay (1x multiplier)
    await page.frameLocator('[id="hanaMainframe"]').locator('[id="inqEndDt"]').fill('2026-05-29'); // Fill in iframe
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('[id="inqStrDt"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(47, 'css');
    }
    // Try multiple selector strategies for resilience (order: css → xpath)
    try {
      // CSS selector
      const locator = page.locator('[id="inqStrDt"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(48, 'css');
    } catch (error0) {
      try {
        // XPath
        const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td/span[1]/input');
        await xpathLocator.hover({ force: true });
        await xpathLocator.click();
        __egdeskRecordStrategy(48, 'xpath');
      }
    }
    // Try multiple selector strategies for resilience (order: css → xpath)
    try {
      // CSS selector
      const locator = page.locator('tr:nth-match(4)');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(49, 'css');
    } catch (error0) {
      try {
        // XPath
        const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]');
        await xpathLocator.hover({ force: true });
        await xpathLocator.click();
        __egdeskRecordStrategy(49, 'xpath');
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('tr');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(50, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    await page.frameLocator('[id="hanaMainframe"]').locator('[id="inqStrDt"]').fill('2026-05-00'); // Fill in iframe
    await page.waitForTimeout(2383); // Human-like delay (1x multiplier)
    await page.frameLocator('[id="hanaMainframe"]').locator('[id="inqStrDt"]').fill('2026-05-01'); // Fill in iframe
    await page.waitForTimeout(2028); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('[id="inqEndDt"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(53, 'css');
    }
    await page.waitForTimeout(1273); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css → xpath)
    try {
      // CSS selector
      const locator = page.locator('td:nth-match(4)');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(54, 'css');
    } catch (error0) {
      try {
        // XPath
        const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[1]/table/tbody/tr[4]/td');
        await xpathLocator.hover({ force: true });
        await xpathLocator.click();
        __egdeskRecordStrategy(54, 'xpath');
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('td');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(55, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('[id="inqEndDt"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(56, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '조회' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(57, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn:nth-match(2)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(57, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[2]/form/fieldset/div/div[2]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(57, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('a:has-text("조회")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(58, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '전체엑셀다운로드' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(59, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn:nth-match(7)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(59, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[2]/div[2]/div[3]/div[2]/div/div[3]/div[2]/a[5]');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(59, 'xpath');
        }
      }
    }
    // Setting up download handler before clicking
    const downloadPromise = page.waitForEvent('download', { timeout: 60000 });
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[id="hanaMainframe"]').locator('a:has-text("전체엑셀다운로드")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(60, 'css');
    }
    // Wait for download to complete
    try {
      const download = await downloadPromise;
      const suggestedFilename = download.suggestedFilename();
      const finalPath = path.resolve(downloadsPath, suggestedFilename);

      // Ensure download directory exists
      if (!fs.existsSync(downloadsPath)) {
        fs.mkdirSync(downloadsPath, { recursive: true });
      }

      // Wait for download to finish
      await page.waitForTimeout(500);

      // Get the temporary file path (more reliable than saveAs on Windows)
      const tempPath = await download.path();

      if (!tempPath) {
        throw new Error('Download path is null - download may have failed');
      }

      console.log('📥 Download temp path:', tempPath);
      console.log('📂 Final destination:', finalPath);

      // Verify temp file exists
      if (!fs.existsSync(tempPath)) {
        throw new Error('Temp file not found: ' + tempPath);
      }

      const tempStats = fs.statSync(tempPath);
      console.log('✅ Temp file exists, size:', tempStats.size, 'bytes');

      // Manual copy with retry logic (handles file locking on Windows)
      let copySuccess = false;
      let lastError = null;
      for (let attempt = 1; attempt <= 3; attempt++) {
        try {
          console.log('📋 Copy attempt ' + attempt + ' of 3...');

          // Use fs.copyFileSync for more reliable Windows support
          fs.copyFileSync(tempPath, finalPath);
          console.log('✅ copyFileSync completed');

          // Verify the destination file exists and has content
          if (fs.existsSync(finalPath)) {
            const stats = fs.statSync(finalPath);
            if (stats.size > 0) {
              console.log('✅ Download completed:', finalPath);
              console.log('📊 File size:', stats.size, 'bytes');
              copySuccess = true;
              break;
            } else {
              throw new Error('Destination file is empty (0 bytes)');
            }
          } else {
            throw new Error('Destination file not found after copy');
          }
        } catch (err) {
          lastError = err;
          console.log('⚠️ Copy attempt ' + attempt + ' failed:', err.message);
          if (attempt < 3) {
            await page.waitForTimeout(1000);
          }
        }
      }

      if (!copySuccess) {
        throw new Error('File copy failed after 3 attempts: ' + (lastError?.message || 'Unknown error'));
      }
    } catch (error) {
      console.error('⚠️ Download wait timed out or failed:', error.message);
      throw error;
    }
    __egdeskMergeSelectorPrefsIntoSpec(__filename, __selectorLearn);
  } finally {
    await context.close();
    // Clean up profile directory
    try {
      fs.rmSync(profileDir, { recursive: true, force: true });
    } catch (e) {
      console.warn('Failed to clean up profile directory:', e);
    }
    // Clean up temporary extensions directory
    if (tempExtensionsDir) {
      try {
        fs.rmSync(tempExtensionsDir, { recursive: true, force: true });
        console.log('🧹 Cleaned up extensions directory');
      } catch (e) {
        console.warn('Failed to clean up extensions directory:', e);
      }
    }
  }
})().catch(console.error);