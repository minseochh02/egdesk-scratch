/**
 * RECORDED_ACTIONS:
 * [{"type":"navigate","url":"https://kiup.ibk.co.kr/uib/jsp/index.jsp","timestamp":1380},{"type":"click","selector":"a:nth-match(22)","xpath":"/html/body/div[10]/div[1]/div[4]/div/ul/li[1]/div/div/div/table/tbody/tr[1]/td[2]/ul/li[1]/a","value":"거래내역조회","innerText":"거래내역조회","role":"a","ariaLabel":"","timestamp":12792},{"type":"click","selector":"a:has-text(\"거래내역조회\")","value":"거래내역조회","timestamp":12793,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":".ec:nth-match(1)","xpath":"/html/body/div[8]/div[3]/div/form/div[2]/div[2]/div/div[2]/div[1]/div[1]/a[3]/span","value":"(구 공인인증서)","innerText":"(구 공인인증서)","role":"span","ariaLabel":"","timestamp":15790},{"type":"click","selector":".ec","value":"(구 공인인증서)","timestamp":15792,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1099)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[2]/ul/li[1]/a","value":"현재 위치 예금","innerText":"현재 위치\n예금","role":"a","ariaLabel":"","timestamp":35065},{"type":"click","selector":"a:has-text(\"현재 위치 예금\")","value":"현재 위치 예금","timestamp":35066,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1100)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[2]/ul/li[2]/a","value":"신탁","innerText":"신탁","role":"a","ariaLabel":"","timestamp":37078},{"type":"click","selector":"a:has-text(\"신탁\")","value":"신탁","timestamp":37080,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1093)","xpath":"/html/body/div[8]/div[4]/div[2]/form[2]/div[2]/ul/li[3]/a","value":"펀드","innerText":"펀드","role":"a","ariaLabel":"","timestamp":41097},{"type":"click","selector":"a:has-text(\"펀드\")","value":"펀드","timestamp":41099,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1102)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[4]/a","value":"대출","innerText":"대출","role":"a","ariaLabel":"","timestamp":43799},{"type":"click","selector":"a:has-text(\"대출\")","value":"대출","timestamp":43802,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1102)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[5]/a","value":"외화","innerText":"외화","role":"a","ariaLabel":"","timestamp":53953},{"type":"click","selector":"a:has-text(\"외화\")","value":"외화","timestamp":53955,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1099)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[4]/ul/li[1]/a","value":"예금","innerText":"예금","role":"a","ariaLabel":"","timestamp":60862},{"type":"click","selector":"a:has-text(\"예금\")","value":"예금","timestamp":60864,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"ecb_user_num01\"]","value":"306-063568-01-061:사무실 시제\n306-063568-04-011:통합통장\n306-063568-04-036:마이너스통장\n306-063568-04-051:기술보증자본금\n306-063568-04-075:자본금증자통장\n306-063568-94-002:직원퇴직연금\n922-001568-01-025:보증부대출전용통장(기보)\n922-001568-04-014:기업자유예금\n922-001568-04-039:기업자유예금\n922-001568-15-113:주거래기업부금(정기적립식)\n922-001568-15-114:주거래기업부금(정기적립식)\n922-001568-15-115:주거래기업부금(정기적립식)\n922-001568-15-116:주거래기업부금(정기적립식)\n922-001568-15-117:주거래기업부금(정기적립식)\n922-001568-15-118:주거래기업부금(정기적립식)\n922-001568-15-119:주거래기업부금(정기적립식)\n922-001568-15-120:상호부금(IBK늘푸른하늘통장(기업)-..\n922-001568-15-121:주거래기업부금(정기적립식)\n922-001568-15-122:주거래기업부금(정기적립식)\n922-001568-15-123:주거래기업부금(정기적립식)\n922-001568-75-011:특정금전신탁(초단기형(MMT))","timestamp":63801,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"ecb_user_num01\"]","value":"306-063568-01-061:사무실 시제\n306-063568-04-011:통합통장\n306-063568-04-036:마이너스통장\n306-063568-04-051:기술보증자본금\n306-063568-04-075:자본금증자통장\n306-063568-94-002:직원퇴직연금\n922-001568-01-025:보증부대출전용통장(기보)\n922-001568-04-014:기업자유예금\n922-001568-04-039:기업자유예금\n922-001568-15-113:주거래기업부금(정기적립식)\n922-001568-15-114:주거래기업부금(정기적립식)\n922-001568-15-115:주거래기업부금(정기적립식)\n922-001568-15-116:주거래기업부금(정기적립식)\n922-001568-15-117:주거래기업부금(정기적립식)\n922-001568-15-118:주거래기업부금(정기적립식)\n922-001568-15-119:주거래기업부금(정기적립식)\n922-001568-15-120:상호부금(IBK늘푸른하늘통장(기업)-..\n922-001568-15-121:주거래기업부금(정기적립식)\n922-001568-15-122:주거래기업부금(정기적립식)\n922-001568-15-123:주거래기업부금(정기적립식)\n922-001568-75-011:특정금전신탁(초단기형(MMT))","timestamp":65513,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd_yy\"]","value":"202120222023202420252026","timestamp":68242,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd_yy\"]","value":"202120222023202420252026","timestamp":69582,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd_yy\"]","value":"202120222023202420252026","timestamp":71221,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd_yy\"]","value":"202120222023202420252026","timestamp":72493,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1100)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[2]/ul/li[2]/a","value":"신탁","innerText":"신탁","role":"a","ariaLabel":"","timestamp":73764},{"type":"click","selector":"a:has-text(\"신탁\")","value":"신탁","timestamp":73766,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"gnrl_lf_acno99\"]","value":"922-001568-75-011:특정금전신탁(초단기형(MMT))","timestamp":76223,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"gnrl_lf_acno99\"]","value":"922-001568-75-011:특정금전신탁(초단기형(MMT))","timestamp":77001,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd1_yy\"]","value":"202120222023202420252026","timestamp":78297,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd1_yy\"]","value":"202120222023202420252026","timestamp":79782,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd1_yy\"]","value":"202120222023202420252026","timestamp":80929,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd1_yy\"]","value":"202120222023202420252026","timestamp":81768,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1093)","xpath":"/html/body/div[8]/div[4]/div[2]/form[2]/div[2]/ul/li[3]/a","value":"펀드","innerText":"펀드","role":"a","ariaLabel":"","timestamp":82695},{"type":"click","selector":"a:has-text(\"펀드\")","value":"펀드","timestamp":82697,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"ecb_user_num01\"]","value":"관련계좌가 없습니다.","timestamp":84799,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"ecb_user_num01\"]","value":"관련계좌가 없습니다.","timestamp":85491,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd1_yy\"]","value":"202120222023202420252026","timestamp":86613,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd1_yy\"]","value":"202120222023202420252026","timestamp":87591,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd1_yy\"]","value":"202120222023202420252026","timestamp":88467,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd1_yy\"]","value":"202120222023202420252026","timestamp":89228,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1102)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[4]/a","value":"대출","innerText":"대출","role":"a","ariaLabel":"","timestamp":90250},{"type":"click","selector":"a:has-text(\"대출\")","value":"대출","timestamp":90252,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"gnrl_lf_acno\"]","value":"306-063568-04-036:마이너스통장\n922-001568-32-00169\n922-001568-32-00170\n922-001568-32-00172\n922-001568-31-00031\n922-001568-32-00177\n922-001568-32-00179\n922-001568-32-00180\n922-001568-32-00197\n922-001568-32-00203\n922-001568-32-00230\n922-001568-32-00247\n922-001568-32-00254\n922-001568-32-00261","timestamp":92558,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"gnrl_lf_acno\"]","value":"306-063568-04-036:마이너스통장\n922-001568-32-00169\n922-001568-32-00170\n922-001568-32-00172\n922-001568-31-00031\n922-001568-32-00177\n922-001568-32-00179\n922-001568-32-00180\n922-001568-32-00197\n922-001568-32-00203\n922-001568-32-00230\n922-001568-32-00247\n922-001568-32-00254\n922-001568-32-00261","timestamp":97902,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd_yy\"]","value":"20162017201820192020202120222023202420252026","timestamp":99357,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd_yy\"]","value":"20162017201820192020202120222023202420252026","timestamp":100352,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd_yy\"]","value":"20162017201820192020202120222023202420252026","timestamp":101197,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd_yy\"]","value":"20162017201820192020202120222023202420252026","timestamp":102159,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"a:nth-match(1102)","xpath":"/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[5]/a","value":"외화","innerText":"외화","role":"a","ariaLabel":"","timestamp":104022},{"type":"click","selector":"a:has-text(\"외화\")","value":"외화","timestamp":104024,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"gnrl_lf_acno11\"]","value":"306-063568-56-00016:(주)원컨덕터","timestamp":106704,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"gnrl_lf_acno11\"]","value":"306-063568-56-00016:(주)원컨덕터","timestamp":107545,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd_yy\"]","value":"202120222023202420252026","timestamp":109415,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_sttg_ymd_yy\"]","value":"202120222023202420252026","timestamp":110372,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd_yy\"]","value":"202120222023202420252026","timestamp":111696,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":"[id=\"inqy_eymd_yy\"]","value":"202120222023202420252026","timestamp":112499,"frameSelector":"[name=\"mainframe\"]"},{"type":"click","selector":".btn_ok","xpath":"/html/body/div[8]/div[4]/div[2]/div[2]/a","value":"조회","innerText":"조회","role":"a","ariaLabel":"","timestamp":113739},{"type":"click","selector":"a:has-text(\"조회\")","value":"조회","timestamp":113742,"frameSelector":"[name=\"mainframe\"]"}]
 *
 * CHROME_EXTENSIONS:
 * []
 */

const { chromium } = require('playwright-core');
const path = require('path');
const os = require('os');
const fs = require('fs');

// Original extension paths from recording session
const EXTENSION_PATHS = [];

/**
 * Copy Chrome extensions to temporary directory
 * Returns { paths: string[], tempDir: string }
 */
function copyExtensionsToTemp(profileDir) {
  if (EXTENSION_PATHS.length === 0) return { paths: [], tempDir: null };
  
  try {
    const tempExtensionsDir = fs.mkdtempSync(path.join(os.tmpdir(), 'egdesk-extensions-'));
    console.log('[Replay] Copying', EXTENSION_PATHS.length, 'extensions to:', tempExtensionsDir);
    
    const copiedPaths = [];
    
    for (const extPath of EXTENSION_PATHS) {
      if (!fs.existsSync(extPath)) {
        console.warn('[Replay] Extension not found:', extPath);
        continue;
      }
      
      const version = path.basename(extPath);
      const extensionId = path.basename(path.dirname(extPath));
      const destPath = path.join(tempExtensionsDir, `${extensionId}-${version}`);
      
      fs.cpSync(extPath, destPath, { recursive: true });
      copiedPaths.push(destPath);
      console.log('[Replay] ✓ Copied:', extensionId);
    }
    
    // Copy native messaging hosts
    copyNativeMessagingHosts(profileDir);
    
    console.log('[Replay] Successfully copied', copiedPaths.length + '/' + EXTENSION_PATHS.length, 'extensions');
    return { paths: copiedPaths, tempDir: tempExtensionsDir };
  } catch (error) {
    console.error('[Replay] Error copying extensions:', error);
    return { paths: [], tempDir: null };
  }
}

/**
 * Copy native messaging host manifests to profile directory
 */
function copyNativeMessagingHosts(profileDir) {
  try {
    const nativeHostLocations = [
      '/Library/Google/Chrome/NativeMessagingHosts',
      '/Library/Application Support/Google/Chrome/NativeMessagingHosts',
      path.join(os.homedir(), 'Library/Application Support/Google/Chrome/NativeMessagingHosts'),
      '/Library/Application Support/Chromium/NativeMessagingHosts',
      path.join(os.homedir(), 'Library/Application Support/Chromium/NativeMessagingHosts')
    ];
    
    const destNativeHostsDir = path.join(profileDir, 'NativeMessagingHosts');
    if (!fs.existsSync(destNativeHostsDir)) {
      fs.mkdirSync(destNativeHostsDir, { recursive: true });
    }
    
    let copiedCount = 0;
    for (const location of nativeHostLocations) {
      if (fs.existsSync(location)) {
        const files = fs.readdirSync(location);
        for (const file of files) {
          if (file.endsWith('.json')) {
            const sourcePath = path.join(location, file);
            const destPath = path.join(destNativeHostsDir, file);
            if (!fs.existsSync(destPath)) {
              fs.copyFileSync(sourcePath, destPath);
              copiedCount++;
            }
          }
        }
      }
    }
    
    if (copiedCount > 0) {
      console.log(`[Replay] ✓ Copied ${copiedCount} native messaging host(s)`);
    }
  } catch (error) {
    console.warn('[Replay] Error copying native hosts:', error);
  }
}

/** Persist preferredLocatorStrategy into this file's RECORDED_ACTIONS comment after a successful run */
function __egdeskMergeSelectorPrefsIntoSpec(filePath, learnMap) {
  if (!learnMap || typeof learnMap !== 'object') return;
  const ks = Object.keys(learnMap);
  if (ks.length === 0) return;
  try {
    const raw = fs.readFileSync(filePath, 'utf8');
    const marker = 'RECORDED_ACTIONS:';
    const idx = raw.indexOf(marker);
    if (idx === -1) return;
    let pos = idx + marker.length;
    while (pos < raw.length && /\s/.test(raw[pos])) pos++;
    while (pos < raw.length && (raw[pos] === '*' || raw[pos] === ' ')) {
      if (raw[pos] === '*') { pos++; while (pos < raw.length && raw[pos] === ' ') pos++; }
      else pos++;
    }
    if (pos >= raw.length || raw[pos] !== '[') return;
    const startBracket = pos;
    let depth = 0, inString = false, stringQuote = null, escape = false;
    let endBracket = -1;
    for (let i = startBracket; i < raw.length; i++) {
      const c = raw[i];
      if (inString) {
        if (escape) { escape = false; continue; }
        if (c === '\') { escape = true; continue; }
        if (stringQuote && c === stringQuote) { inString = false; stringQuote = null; }
        continue;
      }
      if (c === '"' || c === "'") { inString = true; stringQuote = c; continue; }
      if (c === '[') depth++;
      if (c === ']') {
        depth--;
        if (depth === 0) { endBracket = i + 1; break; }
      }
    }
    if (endBracket < 0) return;
    const jsonStr = raw.slice(startBracket, endBracket);
    const actions = JSON.parse(jsonStr);
    for (const k of ks) {
      const ix = parseInt(k, 10);
      if (actions[ix]) actions[ix].preferredLocatorStrategy = learnMap[k];
    }
    const newJson = JSON.stringify(actions);
    fs.writeFileSync(filePath, raw.slice(0, startBracket) + newJson + raw.slice(endBracket), 'utf8');
    console.log('[Replay] 📝 Saved preferredLocatorStrategy for', ks.length, 'action(s)');
  } catch (e) {
    console.warn('[Replay] Could not persist selector preferences:', e && e.message);
  }
}

(async () => {
  console.log('🎬 Starting test replay...');
  
  // Create downloads directory in system Downloads folder (grouped under EGDesk-Browser)
  const downloadsPath = path.join(os.homedir(), 'Downloads', 'EGDesk-Browser', 'ibkbettermethod');
  if (!fs.existsSync(downloadsPath)) {
    fs.mkdirSync(downloadsPath, { recursive: true });
  }
  console.log('📥 Downloads will be saved to:', downloadsPath);

  // Create temporary profile directory
  const profileDir = fs.mkdtempSync(path.join(os.tmpdir(), 'playwright-profile-'));
  console.log('📁 Using profile directory:', profileDir);

  // Copy Chrome extensions if any were used during recording
  let copiedExtensionPaths = [];
  let tempExtensionsDir = null;
  if (EXTENSION_PATHS.length > 0) {
    const extensionResult = copyExtensionsToTemp(profileDir);
    copiedExtensionPaths = extensionResult.paths;
    tempExtensionsDir = extensionResult.tempDir;
  }

  // Launch browser with persistent context (more reliable in production)
  const browserChannel = copiedExtensionPaths.length > 0 ? 'chromium' : 'chrome';
  console.log(`🎭 Launching with channel: ${browserChannel}`);
  if (copiedExtensionPaths.length > 0) {
    console.log('🧩 Loading', copiedExtensionPaths.length, 'Chrome extension(s)');
  }

  // Build browser args
  const args = [
    '--window-size=1152,1032',
    '--window-position=768,0',
    '--no-default-browser-check',
    '--disable-blink-features=AutomationControlled',
    '--no-first-run',
    // Permission handling for localhost and private network access
    '--disable-web-security',
    '--disable-features=IsolateOrigins,site-per-process',
    '--allow-running-insecure-content',
    '--disable-features=PrivateNetworkAccessSendPreflights',
    '--disable-features=PrivateNetworkAccessRespectPreflightResults'
  ];

  // Add extension loading args if extensions were copied
  if (copiedExtensionPaths.length > 0) {
    const extensionPathsStr = copiedExtensionPaths.join(',');
    args.push(`--disable-extensions-except=${extensionPathsStr}`);
    args.push(`--load-extension=${extensionPathsStr}`);
  }

  const context = await chromium.launchPersistentContext(profileDir, {
    headless: false,
    channel: browserChannel,
    viewport: null,
    permissions: ['clipboard-read', 'clipboard-write'],
    acceptDownloads: true,
    downloadsPath: downloadsPath,
    args: args
  });

  // Get or create page
  const pages = context.pages();
  let page = pages.length > 0 ? pages[0] : await context.newPage(); // Use 'let' to allow tab switching
  const pageStack = []; // Track page history for popup close handling

  // Set up dialog handling (auto-accept alerts/confirms for downloads)
  page.on('dialog', async (dialog) => {
    console.log(`🔔 Dialog detected: ${dialog.type()} - "${dialog.message()}"`);
    await dialog.accept();
    console.log('✅ Dialog accepted');
  });

  try {
    const __selectorLearn = {};
    function __egdeskRecordStrategy(actionIndex, strategy) {
      if (strategy) __selectorLearn[actionIndex] = strategy;
    }

    await page.goto('https://kiup.ibk.co.kr/uib/jsp/index.jsp');
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '거래내역조회' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(1, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(22)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(1, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[10]/div[1]/div[4]/div/ul/li[1]/div/div/div/table/tbody/tr[1]/td[2]/ul/li[1]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(1, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("거래내역조회")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(2, 'css');
    }
    await page.waitForTimeout(2997); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('span', { name: '(구 공인인증서)' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(3, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.ec:nth-match(1)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(3, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[3]/div/form/div[2]/div[2]/div/div[2]/div[1]/div[1]/a[3]/span');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(3, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('.ec');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(4, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '현재 위치\n예금' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(5, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1099)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(5, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[2]/ul/li[1]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(5, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("현재 위치 예금")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(6, 'css');
    }
    await page.waitForTimeout(2012); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '신탁' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(7, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1100)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(7, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[2]/ul/li[2]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(7, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("신탁")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(8, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '펀드' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(9, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1093)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(9, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form[2]/div[2]/ul/li[3]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(9, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("펀드")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(10, 'css');
    }
    await page.waitForTimeout(2700); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '대출' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(11, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1102)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(11, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[4]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(11, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("대출")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(12, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '외화' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(13, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1102)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(13, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[5]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(13, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("외화")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(14, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '예금' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(15, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1099)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(15, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[4]/ul/li[1]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(15, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("예금")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(16, 'css');
    }
    await page.waitForTimeout(2937); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="ecb_user_num01"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(17, 'css');
    }
    await page.waitForTimeout(1712); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="ecb_user_num01"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(18, 'css');
    }
    await page.waitForTimeout(2729); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(19, 'css');
    }
    await page.waitForTimeout(1340); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(20, 'css');
    }
    await page.waitForTimeout(1639); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(21, 'css');
    }
    await page.waitForTimeout(1272); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(22, 'css');
    }
    await page.waitForTimeout(1271); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '신탁' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(23, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1100)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(23, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[2]/ul/li[2]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(23, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("신탁")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(24, 'css');
    }
    await page.waitForTimeout(2457); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="gnrl_lf_acno99"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(25, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="gnrl_lf_acno99"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(26, 'css');
    }
    await page.waitForTimeout(1296); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(27, 'css');
    }
    await page.waitForTimeout(1485); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(28, 'css');
    }
    await page.waitForTimeout(1147); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(29, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(30, 'css');
    }
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '펀드' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(31, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1093)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(31, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form[2]/div[2]/ul/li[3]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(31, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("펀드")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(32, 'css');
    }
    await page.waitForTimeout(2102); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="ecb_user_num01"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(33, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="ecb_user_num01"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(34, 'css');
    }
    await page.waitForTimeout(1122); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(35, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(36, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(37, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd1_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(38, 'css');
    }
    await page.waitForTimeout(1022); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '대출' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(39, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1102)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(39, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[4]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(39, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("대출")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(40, 'css');
    }
    await page.waitForTimeout(2306); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="gnrl_lf_acno"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(41, 'css');
    }
    await page.waitForTimeout(3000); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="gnrl_lf_acno"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(42, 'css');
    }
    await page.waitForTimeout(1455); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(43, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(44, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(45, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(46, 'css');
    }
    await page.waitForTimeout(1863); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '외화' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(47, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('a:nth-match(1102)');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(47, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/form/div[3]/ul/li[5]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(47, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("외화")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(48, 'css');
    }
    await page.waitForTimeout(2680); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="gnrl_lf_acno11"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(49, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="gnrl_lf_acno11"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(50, 'css');
    }
    await page.waitForTimeout(1870); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(51, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_sttg_ymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(52, 'css');
    }
    await page.waitForTimeout(1324); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(53, 'css');
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('[id="inqy_eymd_yy"]');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(54, 'css');
    }
    await page.waitForTimeout(1240); // Human-like delay (1x multiplier)
    // Try multiple selector strategies for resilience (order: semantic → css → xpath)
    try {
      // getByRole + text
      const locator = page.getByRole('a', { name: '조회' });
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(55, 'semantic');
    } catch (error0) {
      try {
        // CSS selector
        const locator = page.locator('.btn_ok');
        await locator.hover({ force: true });
        await locator.click({ timeout: 5000 });
        __egdeskRecordStrategy(55, 'css');
      } catch (error1) {
        try {
          // XPath
          const xpathLocator = page.locator('xpath=/html/body/div[8]/div[4]/div[2]/div[2]/a');
          await xpathLocator.hover({ force: true });
          await xpathLocator.click();
          __egdeskRecordStrategy(55, 'xpath');
        }
      }
    }
    // Try multiple selector strategies for resilience (order: css)
    try {
      // CSS selector
      const locator = page.frameLocator('[name="mainframe"]').locator('a:has-text("조회")');
      await locator.hover({ force: true });
      await locator.click({ timeout: 5000 });
      __egdeskRecordStrategy(56, 'css');
    }
    __egdeskMergeSelectorPrefsIntoSpec(__filename, __selectorLearn);
  } finally {
    await context.close();
    // Clean up profile directory
    try {
      fs.rmSync(profileDir, { recursive: true, force: true });
    } catch (e) {
      console.warn('Failed to clean up profile directory:', e);
    }
    // Clean up temporary extensions directory
    if (tempExtensionsDir) {
      try {
        fs.rmSync(tempExtensionsDir, { recursive: true, force: true });
        console.log('🧹 Cleaned up extensions directory');
      } catch (e) {
        console.warn('Failed to clean up extensions directory:', e);
      }
    }
  }
})().catch(console.error);